import App from "./app";
import { TranslateProvider } from "./components/providers/translateProvider";

export default function Home() {
    return (
        // <TranslateProvider>
            <App />
        // </TranslateProvider>
    );
}